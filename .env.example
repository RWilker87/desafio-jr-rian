# ===========================================
# 🐾 Pet Manager - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e ajuste os valores conforme necessário.
# Comando: cp .env.example .env
# ===========================================

# -----------------------------------------
# 🗄️ BANCO DE DADOS (PostgreSQL)
# -----------------------------------------
# URL de conexão com o PostgreSQL
# Formato: postgresql://USUARIO:SENHA@HOST:PORTA/NOME_DO_BANCO?schema=public
#
# 🐳 Para Docker (padrão do docker-compose.yml):
DATABASE_URL="postgresql://postgres:admin@localhost:5432/desafio_jr_rian?schema=public"
#
# 💻 Para PostgreSQL local, ajuste conforme sua instalação:
# DATABASE_URL="postgresql://seu_usuario:sua_senha@localhost:5432/seu_banco?schema=public"

# -----------------------------------------
# 🔐 AUTENTICAÇÃO JWT
# -----------------------------------------
# Chave secreta para assinar os tokens JWT
# ⚠️ IMPORTANTE: Em produção, use uma chave longa, aleatória e segura!
# Você pode gerar uma com: openssl rand -base64 32
JWT_SECRET="troque-por-uma-chave-segura"
